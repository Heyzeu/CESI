version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - REDIS_HOST=redis
      - BUCKET_NAME=mon-bucket-test-local
      # Pour tester l'upload en local, il faudrait monter un volume avec la clé JSON
      # - GOOGLE_APPLICATION_CREDENTIALS=/app/key.json
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
